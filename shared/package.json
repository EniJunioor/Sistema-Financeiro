{
  "name": "@plataforma-financeira/shared",
  "version": "1.0.0",
  "description": "Tipos e utilitários compartilhados entre backend e frontend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}